{
  "name": "neogenesis",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["frontend", "backend", "shared", "engine"],
  "scripts": {
    "dev": "npm run dev -w backend & npm run dev -w frontend",
    "build": "npm run build -w shared & npm run build -w backend & npm run build -w frontend",
    "typecheck": "tsc -b"
  }
}
